{
  "name": "qa-testing-sample",
  "version": "1.0.0",
  "description": "QA Agent testing application with file upload and clipboard UI elements",
  "scripts": {
    "start": "npx serve public -l 3000",
    "build": "echo 'Static site - no build needed'"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
